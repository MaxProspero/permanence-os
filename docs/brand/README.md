# Brand Reference Docs

This directory stores human-facing reference documents for Canon Amendment CA-013.

Files:
- `permanence_systems_brand.docx`
- `permanence_identity_synthesis.docx`

Agent behavior should use `canon/brand_identity.yaml` and `context_loader.py`.
The `.docx` files are source references and are not injected directly into prompts.
